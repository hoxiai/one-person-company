import { index, integer, real, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
import { sql } from "drizzle-orm";
import { bigint, boolean, index as index$1, integer as integer$1, jsonb, pgTable, real as real$1, serial, text as text$1, timestamp, uniqueIndex as uniqueIndex$1 } from "drizzle-orm/pg-core";
import { bigint as bigint$1, boolean as boolean$1, foreignKey, index as index$2, int, json, mysqlTable, real as real$2, text as text$2, timestamp as timestamp$1, uniqueIndex as uniqueIndex$2, varchar } from "drizzle-orm/mysql-core";

//#region rolldown:runtime
var __defProp = Object.defineProperty;
var __exportAll = (all, symbols) => {
	let target = {};
	for (var name in all) {
		__defProp(target, name, {
			get: all[name],
			enumerable: true
		});
	}
	if (symbols) {
		__defProp(target, Symbol.toStringTag, { value: "Module" });
	}
	return target;
};

//#endregion
//#region server/db/schema.sqlite.ts
var schema_sqlite_exports = /* @__PURE__ */ __exportAll({
	accessLogs: () => accessLogs$3,
	adminTokens: () => adminTokens$3,
	admins: () => admins$3,
	balanceLogs: () => balanceLogs$3,
	cards: () => cards$3,
	emailLogs: () => emailLogs$3,
	emailProviders: () => emailProviders$3,
	eventRules: () => eventRules$3,
	failures: () => failures$3,
	logs: () => logs$3,
	notifications: () => notifications$3,
	oauthAccounts: () => oauthAccounts$3,
	operationLogs: () => operationLogs$3,
	orders: () => orders$3,
	paymentFailures: () => paymentFailures$3,
	paymentMethods: () => paymentMethods$3,
	posts: () => posts$3,
	products: () => products$3,
	promoAgentRelations: () => promoAgentRelations$3,
	promoAgentTiers: () => promoAgentTiers$3,
	promoApplications: () => promoApplications$3,
	promoCommissions: () => promoCommissions$3,
	promoInviteRelations: () => promoInviteRelations$3,
	promoMembers: () => promoMembers$3,
	promoOrderAttributions: () => promoOrderAttributions$3,
	settings: () => settings$3,
	subscriptions: () => subscriptions$3,
	ticketMessages: () => ticketMessages$3,
	tickets: () => tickets$3,
	topups: () => topups$3,
	userSessions: () => userSessions$3,
	userTokens: () => userTokens$3,
	userWallets: () => userWallets$3,
	users: () => users$3,
	visitorEvents: () => visitorEvents$3,
	visitorProfiles: () => visitorProfiles$3,
	webhooks: () => webhooks$3
});
const users$3 = sqliteTable("users", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	email: text("email").notNull().unique(),
	passwordHash: text("password_hash"),
	nickname: text("nickname"),
	avatarUrl: text("avatar_url"),
	lastLoginAt: integer("last_login_at", { mode: "timestamp" }),
	currentSessionId: text("current_session_id"),
	status: integer("status").default(1),
	emailVerifiedAt: integer("email_verified_at", { mode: "timestamp" }),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const userWallets$3 = sqliteTable("user_wallets", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	userId: integer("user_id").notNull().references(() => users$3.id, { onDelete: "cascade" }).unique(),
	cashBalance: integer("cash_balance", { mode: "number" }).notNull().default(0),
	grantBalance: integer("grant_balance", { mode: "number" }).notNull().default(0),
	subBalance: integer("sub_balance", { mode: "number" }).notNull().default(0),
	pointsBalance: integer("points_balance", { mode: "number" }).notNull().default(0),
	tierLevel: integer("tier_level", { mode: "number" }).notNull().default(0),
	subExpiresAt: integer("sub_expires_at", { mode: "timestamp" }),
	status: integer("status").notNull().default(1),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const userSessions$3 = sqliteTable("user_sessions", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	userId: integer("user_id").notNull().references(() => users$3.id),
	sessionIdHash: text("session_id_hash").notNull().unique(),
	status: text("status").notNull().default("active"),
	authMethod: text("auth_method").notNull().default("password"),
	deviceType: text("device_type"),
	browser: text("browser"),
	os: text("os"),
	userAgent: text("user_agent"),
	ip: text("ip"),
	country: text("country"),
	region: text("region"),
	city: text("city"),
	loggedInAt: integer("logged_in_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	lastSeenAt: integer("last_seen_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	endedAt: integer("ended_at", { mode: "timestamp" }),
	replacedBySessionId: text("replaced_by_session_id"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
}, (table) => ({
	userStatusIdx: index("user_sessions_user_status_idx").on(table.userId, table.status),
	lastSeenIdx: index("user_sessions_last_seen_idx").on(table.lastSeenAt)
}));
const userTokens$3 = sqliteTable("user_tokens", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	userId: integer("user_id").notNull().references(() => users$3.id),
	token: text("token").notNull().unique(),
	name: text("name"),
	expiresAt: integer("expires_at", { mode: "timestamp" }),
	lastUsedAt: integer("last_used_at", { mode: "timestamp" }),
	revoked: integer("revoked").notNull().default(0),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const admins$3 = sqliteTable("admins", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	username: text("username").notNull().unique(),
	passwordHash: text("password_hash").notNull(),
	permissions: text("permissions", { mode: "json" }).$type(),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const adminTokens$3 = sqliteTable("admin_tokens", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	adminId: integer("admin_id").notNull().references(() => admins$3.id),
	token: text("token").notNull().unique(),
	name: text("name"),
	permissions: text("permissions", { mode: "json" }).$type(),
	expiresAt: integer("expires_at", { mode: "timestamp" }),
	lastUsedAt: integer("last_used_at", { mode: "timestamp" }),
	revoked: integer("revoked").notNull().default(0),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const oauthAccounts$3 = sqliteTable("oauth_accounts", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	userId: integer("user_id").notNull().references(() => users$3.id),
	provider: text("provider").notNull(),
	providerAccountId: text("provider_account_id").notNull(),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const products$3 = sqliteTable("products", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	slug: text("slug").unique(),
	name: text("name").notNull(),
	price: real("price").notNull(),
	description: text("description"),
	content: text("content"),
	type: text("type").notNull(),
	imageUrl: text("image_url"),
	views: integer("views").notNull().default(0),
	imageUrls: text("image_urls", { mode: "json" }).$type(),
	resource: text("resource"),
	isActive: integer("is_active", { mode: "boolean" }).notNull().default(true),
	status: text("status").notNull().default("active"),
	metaData: text("meta_data", { mode: "json" }),
	sortOrder: integer("sort_order").notNull().default(0),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const cards$3 = sqliteTable("cards", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	productId: integer("product_id").notNull().references(() => products$3.id),
	cardNumber: text("card_number").notNull(),
	isUsed: integer("is_used", { mode: "boolean" }).notNull().default(false),
	orderId: text("order_id"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const paymentMethods$3 = sqliteTable("payment_methods", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	name: text("name").notNull(),
	code: text("code").notNull(),
	iconUrl: text("icon_url"),
	isActive: integer("is_active", { mode: "boolean" }).notNull().default(false),
	supportedLocales: text("supported_locales"),
	configJson: text("config_json"),
	info: text("info"),
	create: text("create"),
	callback: text("callback"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const emailProviders$3 = sqliteTable("email_providers", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	name: text("name").notNull(),
	code: text("code").notNull(),
	isActive: integer("is_active", { mode: "boolean" }).notNull().default(false),
	configJson: text("config_json"),
	sendScript: text("send_script"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const emailLogs$3 = sqliteTable("email_logs", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	to: text("to").notNull(),
	subject: text("subject").notNull(),
	templateCode: text("template_code"),
	html: text("html"),
	provider: text("provider"),
	status: text("status").notNull().default("success"),
	messageId: text("message_id"),
	error: text("error"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const orders$3 = sqliteTable("orders", {
	id: text("id").primaryKey(),
	amount: real("amount").notNull(),
	currency: text("currency").notNull().default("USD"),
	source: text("source"),
	externalOrderId: text("external_order_id"),
	productId: integer("product_id").notNull().references(() => products$3.id),
	userId: integer("user_id").references(() => users$3.id),
	contactEmail: text("contact_email").notNull(),
	payMethod: text("pay_method"),
	tradeNo: text("trade_no"),
	status: text("status").notNull().default("none"),
	deliveryInfo: text("delivery_info"),
	metaData: text("meta_data", { mode: "json" }),
	visitorId: text("visitor_id"),
	subscriptionId: text("subscription_id"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	paidAt: integer("paid_at", { mode: "timestamp" }),
	payStatus: text("pay_status").notNull().default("pending")
}, (table) => [uniqueIndex("orders_source_external_order_unique").on(table.source, table.externalOrderId)]);
const topups$3 = sqliteTable("topups", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	orderId: text("order_id").notNull().references(() => orders$3.id, { onDelete: "cascade" }).unique(),
	userId: integer("user_id").notNull().references(() => users$3.id),
	walletId: integer("wallet_id").notNull().references(() => userWallets$3.id),
	source: text("source").notNull().default("order"),
	paymentAmount: real("payment_amount").notNull(),
	paymentCurrency: text("payment_currency").notNull(),
	creditAmountCents: integer("credit_amount_cents", { mode: "number" }).notNull(),
	creditCurrency: text("credit_currency").notNull(),
	exchangeRate: real("exchange_rate").notNull().default(1),
	balanceType: text("balance_type").notNull().default("cash"),
	status: text("status").notNull().default("pending"),
	creditEventId: text("credit_event_id").notNull().unique(),
	refundEventId: text("refund_event_id").unique(),
	retryCount: integer("retry_count").notNull().default(0),
	shortfallCents: integer("shortfall_cents", { mode: "number" }).notNull().default(0),
	lastError: text("last_error"),
	paidAt: integer("paid_at", { mode: "timestamp" }),
	creditedAt: integer("credited_at", { mode: "timestamp" }),
	refundedAt: integer("refunded_at", { mode: "timestamp" }),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
}, (table) => ({
	userCreatedAtIdx: index("idx_topups_user_created_at").on(table.userId, table.createdAt),
	statusUpdatedAtIdx: index("idx_topups_status_updated_at").on(table.status, table.updatedAt)
}));
const subscriptions$3 = sqliteTable("subscriptions", {
	id: text("id").primaryKey(),
	gatewaySubId: text("gateway_sub_id"),
	userId: integer("user_id").references(() => users$3.id),
	productId: integer("product_id").notNull().references(() => products$3.id),
	payMethod: text("pay_method").notNull(),
	status: text("status").notNull().default("active"),
	interval: text("interval").notNull(),
	intervalCount: integer("interval_count").notNull().default(1),
	amount: real("amount").notNull(),
	currency: text("currency").notNull().default("USD"),
	currentPeriodStart: integer("current_period_start", { mode: "timestamp" }),
	currentPeriodEnd: integer("current_period_end", { mode: "timestamp" }),
	cancelAtPeriodEnd: integer("cancel_at_period_end", { mode: "boolean" }).default(false),
	metaData: text("meta_data", { mode: "json" }),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).default(sql`(unixepoch())`)
});
const settings$3 = sqliteTable("settings", {
	key: text("key").primaryKey(),
	value: text("value").notNull(),
	description: text("description"),
	updatedAt: integer("updated_at", { mode: "timestamp" }).default(sql`(unixepoch() * 1000)`)
});
const paymentFailures$3 = sqliteTable("payment_failures", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	orderId: text("order_id").notNull(),
	cardBin: text("card_bin"),
	reason: text("reason").notNull(),
	amount: real("amount"),
	payMethod: text("pay_method"),
	contactEmail: text("contact_email"),
	rawResponse: text("raw_response"),
	visitorId: text("visitor_id"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const failures$3 = paymentFailures$3;
const webhooks$3 = sqliteTable("webhooks", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	name: text("name").notNull(),
	url: text("url").notNull(),
	events: text("events", { mode: "json" }).$type(),
	secret: text("secret"),
	isActive: integer("is_active", { mode: "boolean" }).notNull().default(true),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const eventRules$3 = sqliteTable("event_rules", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	event: text("event").notNull(),
	action: text("action").notNull(),
	config: text("config", { mode: "json" }).$type(),
	enabled: integer("enabled", { mode: "boolean" }).notNull().default(true),
	remark: text("remark"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" })
});
const logs$3 = sqliteTable("logs", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	level: text("level").notNull().default("info"),
	message: text("message").notNull(),
	details: text("details"),
	source: text("source"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const visitorProfiles$3 = sqliteTable("visitor_profiles", {
	visitorId: text("visitor_id").primaryKey(),
	userId: integer("user_id").references(() => users$3.id),
	ip: text("ip"),
	firstSeenAt: integer("first_seen_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	lastSeenAt: integer("last_seen_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	landingPath: text("landing_path"),
	firstPath: text("first_path"),
	lastPath: text("last_path"),
	firstReferrer: text("first_referrer"),
	lastReferrer: text("last_referrer"),
	firstSourceType: text("first_source_type"),
	lastSourceType: text("last_source_type"),
	firstSource: text("first_source"),
	lastSource: text("last_source"),
	firstMedium: text("first_medium"),
	lastMedium: text("last_medium"),
	firstCampaign: text("first_campaign"),
	lastCampaign: text("last_campaign"),
	firstContent: text("first_content"),
	lastContent: text("last_content"),
	firstTerm: text("first_term"),
	lastTerm: text("last_term"),
	country: text("country"),
	region: text("region"),
	city: text("city"),
	locale: text("locale"),
	currency: text("currency"),
	deviceType: text("device_type"),
	browser: text("browser"),
	os: text("os"),
	userAgent: text("user_agent"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const visitorEvents$3 = sqliteTable("visitor_events", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	visitorId: text("visitor_id").notNull(),
	ip: text("ip"),
	userId: integer("user_id").references(() => users$3.id),
	orderId: text("order_id").references(() => orders$3.id),
	productId: integer("product_id").references(() => products$3.id),
	eventName: text("event_name").notNull(),
	eventAction: text("event_action"),
	path: text("path"),
	referrer: text("referrer"),
	sourceType: text("source_type"),
	source: text("source"),
	medium: text("medium"),
	campaign: text("campaign"),
	content: text("content"),
	term: text("term"),
	country: text("country"),
	region: text("region"),
	city: text("city"),
	locale: text("locale"),
	currency: text("currency"),
	deviceType: text("device_type"),
	browser: text("browser"),
	os: text("os"),
	userAgent: text("user_agent"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const accessLogs$3 = sqliteTable("access_logs", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	path: text("path").notNull(),
	method: text("method").notNull(),
	ip: text("ip"),
	userAgent: text("user_agent"),
	referrer: text("referrer"),
	country: text("country"),
	region: text("region"),
	city: text("city"),
	statusCode: integer("status_code"),
	duration: real("duration"),
	visitorId: text("visitor_id"),
	userId: integer("user_id").references(() => users$3.id),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const operationLogs$3 = sqliteTable("operation_logs", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	actorType: text("actor_type").notNull().default("admin"),
	actorId: integer("actor_id"),
	actorName: text("actor_name"),
	action: text("action").notNull(),
	resource: text("resource").notNull(),
	resourceId: text("resource_id"),
	summary: text("summary"),
	details: text("details"),
	path: text("path").notNull(),
	method: text("method").notNull(),
	statusCode: integer("status_code"),
	ip: text("ip"),
	userAgent: text("user_agent"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
}, (table) => ({
	createdAtIdx: index("operation_logs_created_at_idx").on(table.createdAt),
	actorIdx: index("operation_logs_actor_idx").on(table.actorId, table.createdAt),
	resourceIdx: index("operation_logs_resource_idx").on(table.resource, table.resourceId)
}));
const posts$3 = sqliteTable("posts", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	key: text("key"),
	sort: integer("sort"),
	slug: text("slug").notNull().unique(),
	title: text("title").notNull(),
	description: text("description"),
	content: text("content"),
	type: text("type").notNull().default("blog"),
	imageUrl: text("image_url"),
	views: integer("views").notNull().default(0),
	isActive: integer("is_active", { mode: "boolean" }).notNull().default(true),
	metaData: text("meta_data", { mode: "json" }),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).default(sql`(unixepoch())`)
});
const notifications$3 = sqliteTable("notifications", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	userId: integer("user_id").references(() => users$3.id),
	visitorId: text("visitor_id"),
	type: text("type").notNull(),
	title: text("title").notNull(),
	message: text("message").notNull(),
	data: text("data", { mode: "json" }),
	isRead: integer("is_read", { mode: "boolean" }).notNull().default(false),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const balanceLogs$3 = sqliteTable("balance_logs", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	userId: integer("user_id").notNull().references(() => users$3.id),
	walletId: integer("wallet_id").notNull().references(() => userWallets$3.id),
	balanceType: text("balance_type").notNull(),
	actionType: text("action_type").notNull().default("topup"),
	amountCents: integer("amount_cents", { mode: "number" }).notNull(),
	beforeBalanceCents: integer("before_balance_cents", { mode: "number" }).notNull(),
	afterBalanceCents: integer("after_balance_cents", { mode: "number" }).notNull(),
	eventId: text("event_id").notNull().unique(),
	sourceType: text("source_type").notNull().default("system"),
	sourceId: text("source_id"),
	operatorAdminId: integer("operator_admin_id"),
	operatorName: text("operator_name").notNull().default(""),
	remark: text("remark").notNull().default(""),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
}, (table) => ({ walletCreatedAtIdx: index("idx_balance_logs_wallet_created_at").on(table.walletId, table.createdAt) }));
const promoMembers$3 = sqliteTable("promo_members", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	userId: integer("user_id").notNull().references(() => users$3.id).unique(),
	role: text("role").notNull().default("member"),
	status: text("status").notNull().default("active"),
	promoCode: text("promo_code").notNull().unique(),
	inviteCode: text("invite_code").notNull().unique(),
	agentCode: text("agent_code").unique(),
	currentAgentTierId: integer("current_agent_tier_id").references(() => promoAgentTiers$3.id),
	joinedAt: integer("joined_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const promoInviteRelations$3 = sqliteTable("promo_invite_relations", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	inviteeUserId: integer("invitee_user_id").notNull().references(() => users$3.id).unique(),
	inviterUserId: integer("inviter_user_id").notNull().references(() => users$3.id),
	source: text("source").notNull().default("register"),
	codeSnapshot: text("code_snapshot"),
	boundAt: integer("bound_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const promoAgentTiers$3 = sqliteTable("promo_agent_tiers", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	code: text("code").notNull().unique(),
	name: text("name").notNull(),
	roleScope: text("role_scope").notNull().default("agent"),
	level: integer("level").notNull().default(1),
	discountRate: real("discount_rate").notNull().default(1),
	salesThreshold: real("sales_threshold").notNull().default(0),
	isFixed: integer("is_fixed", { mode: "boolean" }).notNull().default(false),
	isActive: integer("is_active", { mode: "boolean" }).notNull().default(true),
	description: text("description"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const promoAgentRelations$3 = sqliteTable("promo_agent_relations", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	agentUserId: integer("agent_user_id").notNull().references(() => users$3.id).unique(),
	parentAgentUserId: integer("parent_agent_user_id").references(() => users$3.id),
	masterAgentUserId: integer("master_agent_user_id").references(() => users$3.id),
	depth: integer("depth").notNull().default(1),
	status: text("status").notNull().default("active"),
	boundAt: integer("bound_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const promoOrderAttributions$3 = sqliteTable("promo_order_attributions", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	orderId: text("order_id").notNull().references(() => orders$3.id).unique(),
	buyerUserId: integer("buyer_user_id").references(() => users$3.id),
	buyerPromoMemberId: integer("buyer_promo_member_id").references(() => promoMembers$3.id),
	inviteUserId: integer("invite_user_id").references(() => users$3.id),
	agentUserId: integer("agent_user_id").references(() => users$3.id),
	parentAgentUserId: integer("parent_agent_user_id").references(() => users$3.id),
	masterAgentUserId: integer("master_agent_user_id").references(() => users$3.id),
	agentTierIdSnapshot: integer("agent_tier_id_snapshot"),
	agentTierNameSnapshot: text("agent_tier_name_snapshot"),
	discountRateSnapshot: real("discount_rate_snapshot"),
	sourceType: text("source_type").notNull().default("direct"),
	metaData: text("meta_data", { mode: "json" }),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const promoCommissions$3 = sqliteTable("promo_commissions", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	orderId: text("order_id").notNull().references(() => orders$3.id),
	ownerUserId: integer("owner_user_id").notNull().references(() => users$3.id),
	ownerPromoMemberId: integer("owner_promo_member_id").references(() => promoMembers$3.id),
	type: text("type").notNull(),
	sourceType: text("source_type").notNull().default("direct"),
	amount: real("amount").notNull(),
	rate: real("rate"),
	status: text("status").notNull().default("pending"),
	remark: text("remark"),
	metaData: text("meta_data", { mode: "json" }),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
}, (table) => ({ orderTypeIdx: uniqueIndex("promo_commissions_order_type_idx").on(table.orderId, table.type) }));
const promoApplications$3 = sqliteTable("promo_applications", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	userId: integer("user_id").notNull().references(() => users$3.id),
	status: text("status").notNull().default("pending"),
	channelInfo: text("channel_info"),
	contact: text("contact"),
	reason: text("reason"),
	reviewNote: text("review_note"),
	reviewedByAdminId: integer("reviewed_by_admin_id"),
	reviewedAt: integer("reviewed_at", { mode: "timestamp" }),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
});
const tickets$3 = sqliteTable("tickets", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	ticketNo: text("ticket_no").notNull().unique(),
	userId: integer("user_id").notNull().references(() => users$3.id),
	category: text("category").notNull().default("other"),
	title: text("title").notNull(),
	status: text("status").notNull().default("open"),
	priority: text("priority").notNull().default("normal"),
	context: text("context", { mode: "json" }),
	lastRepliedAt: integer("last_replied_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	lastRepliedBy: text("last_replied_by").notNull().default("user"),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`),
	updatedAt: integer("updated_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
}, (table) => ({
	userIdIdx: index("tickets_user_id_idx").on(table.userId),
	statusIdx: index("tickets_status_idx").on(table.status),
	categoryIdx: index("tickets_category_idx").on(table.category),
	lastRepliedAtIdx: index("tickets_last_replied_at_idx").on(table.lastRepliedAt)
}));
const ticketMessages$3 = sqliteTable("ticket_messages", {
	id: integer("id").primaryKey({ autoIncrement: true }),
	ticketId: integer("ticket_id").notNull().references(() => tickets$3.id, { onDelete: "cascade" }),
	senderType: text("sender_type").notNull(),
	senderId: integer("sender_id"),
	senderName: text("sender_name").notNull().default(""),
	content: text("content").notNull(),
	attachments: text("attachments", { mode: "json" }),
	createdAt: integer("created_at", { mode: "timestamp" }).notNull().default(sql`(unixepoch())`)
}, (table) => ({
	ticketIdIdx: index("ticket_messages_ticket_id_idx").on(table.ticketId),
	createdAtIdx: index("ticket_messages_created_at_idx").on(table.createdAt)
}));

//#endregion
//#region server/db/schema.pg.ts
var schema_pg_exports = /* @__PURE__ */ __exportAll({
	accessLogs: () => accessLogs$2,
	adminTokens: () => adminTokens$2,
	admins: () => admins$2,
	balanceLogs: () => balanceLogs$2,
	cards: () => cards$2,
	emailLogs: () => emailLogs$2,
	emailProviders: () => emailProviders$2,
	eventRules: () => eventRules$2,
	failures: () => failures$2,
	logs: () => logs$2,
	notifications: () => notifications$2,
	oauthAccounts: () => oauthAccounts$2,
	operationLogs: () => operationLogs$2,
	orders: () => orders$2,
	paymentFailures: () => paymentFailures$2,
	paymentMethods: () => paymentMethods$2,
	posts: () => posts$2,
	products: () => products$2,
	promoAgentRelations: () => promoAgentRelations$2,
	promoAgentTiers: () => promoAgentTiers$2,
	promoApplications: () => promoApplications$2,
	promoCommissions: () => promoCommissions$2,
	promoInviteRelations: () => promoInviteRelations$2,
	promoMembers: () => promoMembers$2,
	promoOrderAttributions: () => promoOrderAttributions$2,
	settings: () => settings$2,
	subscriptions: () => subscriptions$2,
	ticketMessages: () => ticketMessages$2,
	tickets: () => tickets$2,
	topups: () => topups$2,
	userSessions: () => userSessions$2,
	userTokens: () => userTokens$2,
	userWallets: () => userWallets$2,
	users: () => users$2,
	visitorEvents: () => visitorEvents$2,
	visitorProfiles: () => visitorProfiles$2,
	webhooks: () => webhooks$2
});
const users$2 = pgTable("users", {
	id: serial("id").primaryKey(),
	email: text$1("email").notNull().unique(),
	passwordHash: text$1("password_hash"),
	nickname: text$1("nickname"),
	avatarUrl: text$1("avatar_url"),
	lastLoginAt: timestamp("last_login_at", { withTimezone: true }),
	currentSessionId: text$1("current_session_id"),
	status: integer$1("status").default(1),
	emailVerifiedAt: timestamp("email_verified_at", { withTimezone: true }),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const userWallets$2 = pgTable("user_wallets", {
	id: serial("id").primaryKey(),
	userId: integer$1("user_id").notNull().references(() => users$2.id, { onDelete: "cascade" }).unique(),
	cashBalance: bigint("cash_balance", { mode: "bigint" }).notNull().default(sql`0`),
	grantBalance: bigint("grant_balance", { mode: "bigint" }).notNull().default(sql`0`),
	subBalance: bigint("sub_balance", { mode: "bigint" }).notNull().default(sql`0`),
	pointsBalance: bigint("points_balance", { mode: "bigint" }).notNull().default(sql`0`),
	tierLevel: integer$1("tier_level").notNull().default(0),
	subExpiresAt: timestamp("sub_expires_at", { withTimezone: true }),
	status: integer$1("status").notNull().default(1),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const userSessions$2 = pgTable("user_sessions", {
	id: serial("id").primaryKey(),
	userId: integer$1("user_id").notNull().references(() => users$2.id),
	sessionIdHash: text$1("session_id_hash").notNull().unique(),
	status: text$1("status").notNull().default("active"),
	authMethod: text$1("auth_method").notNull().default("password"),
	deviceType: text$1("device_type"),
	browser: text$1("browser"),
	os: text$1("os"),
	userAgent: text$1("user_agent"),
	ip: text$1("ip"),
	country: text$1("country"),
	region: text$1("region"),
	city: text$1("city"),
	loggedInAt: timestamp("logged_in_at", { withTimezone: true }).notNull().defaultNow(),
	lastSeenAt: timestamp("last_seen_at", { withTimezone: true }).notNull().defaultNow(),
	endedAt: timestamp("ended_at", { withTimezone: true }),
	replacedBySessionId: text$1("replaced_by_session_id"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
}, (table) => ({
	userStatusIdx: index$1("user_sessions_user_status_idx").on(table.userId, table.status),
	lastSeenIdx: index$1("user_sessions_last_seen_idx").on(table.lastSeenAt)
}));
const userTokens$2 = pgTable("user_tokens", {
	id: serial("id").primaryKey(),
	userId: integer$1("user_id").notNull().references(() => users$2.id),
	token: text$1("token").notNull().unique(),
	name: text$1("name"),
	expiresAt: timestamp("expires_at", { withTimezone: true }),
	lastUsedAt: timestamp("last_used_at", { withTimezone: true }),
	revoked: boolean("revoked").notNull().default(false),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const admins$2 = pgTable("admins", {
	id: serial("id").primaryKey(),
	username: text$1("username").notNull().unique(),
	passwordHash: text$1("password_hash").notNull(),
	permissions: jsonb("permissions").$type(),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const adminTokens$2 = pgTable("admin_tokens", {
	id: serial("id").primaryKey(),
	adminId: integer$1("admin_id").notNull().references(() => admins$2.id),
	token: text$1("token").notNull().unique(),
	name: text$1("name"),
	permissions: jsonb("permissions").$type(),
	expiresAt: timestamp("expires_at", { withTimezone: true }),
	lastUsedAt: timestamp("last_used_at", { withTimezone: true }),
	revoked: boolean("revoked").notNull().default(false),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const oauthAccounts$2 = pgTable("oauth_accounts", {
	id: serial("id").primaryKey(),
	userId: integer$1("user_id").notNull().references(() => users$2.id),
	provider: text$1("provider").notNull(),
	providerAccountId: text$1("provider_account_id").notNull(),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
}, (table) => {
	return { providerIdx: uniqueIndex$1("provider_account_idx").on(table.provider, table.providerAccountId) };
});
const products$2 = pgTable("products", {
	id: serial("id").primaryKey(),
	slug: text$1("slug").unique(),
	name: text$1("name").notNull(),
	price: real$1("price").notNull(),
	description: text$1("description"),
	content: text$1("content"),
	type: text$1("type").notNull(),
	imageUrl: text$1("image_url"),
	views: integer$1("views").notNull().default(0),
	imageUrls: jsonb("image_urls").$type(),
	resource: text$1("resource"),
	isActive: boolean("is_active").notNull().default(true),
	status: text$1("status").notNull().default("active"),
	metaData: jsonb("meta_data"),
	sortOrder: integer$1("sort_order").notNull().default(0),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const cards$2 = pgTable("cards", {
	id: serial("id").primaryKey(),
	productId: integer$1("product_id").notNull().references(() => products$2.id),
	cardNumber: text$1("card_number").notNull(),
	isUsed: boolean("is_used").notNull().default(false),
	orderId: text$1("order_id"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const paymentMethods$2 = pgTable("payment_methods", {
	id: serial("id").primaryKey(),
	name: text$1("name").notNull(),
	code: text$1("code").notNull(),
	iconUrl: text$1("icon_url"),
	isActive: boolean("is_active").notNull().default(false),
	supportedLocales: text$1("supported_locales"),
	configJson: text$1("config_json"),
	info: text$1("info"),
	create: text$1("create"),
	callback: text$1("callback"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const emailProviders$2 = pgTable("email_providers", {
	id: serial("id").primaryKey(),
	name: text$1("name").notNull(),
	code: text$1("code").notNull(),
	isActive: boolean("is_active").notNull().default(false),
	configJson: text$1("config_json"),
	sendScript: text$1("send_script"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const emailLogs$2 = pgTable("email_logs", {
	id: serial("id").primaryKey(),
	to: text$1("to").notNull(),
	subject: text$1("subject").notNull(),
	templateCode: text$1("template_code"),
	html: text$1("html"),
	provider: text$1("provider"),
	status: text$1("status").notNull().default("success"),
	messageId: text$1("message_id"),
	error: text$1("error"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const orders$2 = pgTable("orders", {
	id: text$1("id").primaryKey(),
	amount: real$1("amount").notNull(),
	currency: text$1("currency").notNull().default("USD"),
	source: text$1("source"),
	externalOrderId: text$1("external_order_id"),
	productId: integer$1("product_id").notNull().references(() => products$2.id),
	userId: integer$1("user_id").references(() => users$2.id),
	contactEmail: text$1("contact_email").notNull(),
	payMethod: text$1("pay_method"),
	tradeNo: text$1("trade_no"),
	status: text$1("status").notNull().default("none"),
	deliveryInfo: text$1("delivery_info"),
	metaData: jsonb("meta_data"),
	visitorId: text$1("visitor_id"),
	subscriptionId: text$1("subscription_id"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	paidAt: timestamp("paid_at", { withTimezone: true }),
	payStatus: text$1("pay_status").notNull().default("pending")
}, (table) => [uniqueIndex$1("orders_source_external_order_unique").on(table.source, table.externalOrderId)]);
const topups$2 = pgTable("topups", {
	id: serial("id").primaryKey(),
	orderId: text$1("order_id").notNull().references(() => orders$2.id, { onDelete: "cascade" }).unique(),
	userId: integer$1("user_id").notNull().references(() => users$2.id),
	walletId: integer$1("wallet_id").notNull().references(() => userWallets$2.id),
	source: text$1("source").notNull().default("order"),
	paymentAmount: real$1("payment_amount").notNull(),
	paymentCurrency: text$1("payment_currency").notNull(),
	creditAmountCents: bigint("credit_amount_cents", { mode: "bigint" }).notNull(),
	creditCurrency: text$1("credit_currency").notNull(),
	exchangeRate: real$1("exchange_rate").notNull().default(1),
	balanceType: text$1("balance_type").notNull().default("cash"),
	status: text$1("status").notNull().default("pending"),
	creditEventId: text$1("credit_event_id").notNull().unique(),
	refundEventId: text$1("refund_event_id").unique(),
	retryCount: integer$1("retry_count").notNull().default(0),
	shortfallCents: bigint("shortfall_cents", { mode: "bigint" }).notNull().default(sql`0`),
	lastError: text$1("last_error"),
	paidAt: timestamp("paid_at", { withTimezone: true }),
	creditedAt: timestamp("credited_at", { withTimezone: true }),
	refundedAt: timestamp("refunded_at", { withTimezone: true }),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
}, (table) => ({
	userCreatedAtIdx: index$1("idx_topups_user_created_at").on(table.userId, table.createdAt),
	statusUpdatedAtIdx: index$1("idx_topups_status_updated_at").on(table.status, table.updatedAt)
}));
const subscriptions$2 = pgTable("subscriptions", {
	id: text$1("id").primaryKey(),
	gatewaySubId: text$1("gateway_sub_id"),
	userId: integer$1("user_id").references(() => users$2.id),
	productId: integer$1("product_id").notNull().references(() => products$2.id),
	payMethod: text$1("pay_method").notNull(),
	status: text$1("status").notNull().default("active"),
	interval: text$1("interval").notNull(),
	intervalCount: integer$1("interval_count").notNull().default(1),
	amount: real$1("amount").notNull(),
	currency: text$1("currency").notNull().default("USD"),
	currentPeriodStart: timestamp("current_period_start", { withTimezone: true }),
	currentPeriodEnd: timestamp("current_period_end", { withTimezone: true }),
	cancelAtPeriodEnd: boolean("cancel_at_period_end").default(false),
	metaData: jsonb("meta_data"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow()
});
const settings$2 = pgTable("settings", {
	key: text$1("key").primaryKey(),
	value: text$1("value").notNull(),
	description: text$1("description"),
	updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow()
});
const paymentFailures$2 = pgTable("payment_failures", {
	id: serial("id").primaryKey(),
	orderId: text$1("order_id").notNull(),
	cardBin: text$1("card_bin"),
	reason: text$1("reason").notNull(),
	amount: real$1("amount"),
	payMethod: text$1("pay_method"),
	contactEmail: text$1("contact_email"),
	rawResponse: text$1("raw_response"),
	visitorId: text$1("visitor_id"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const failures$2 = paymentFailures$2;
const webhooks$2 = pgTable("webhooks", {
	id: serial("id").primaryKey(),
	name: text$1("name").notNull(),
	url: text$1("url").notNull(),
	events: jsonb("events").$type(),
	secret: text$1("secret"),
	isActive: boolean("is_active").notNull().default(true),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const eventRules$2 = pgTable("event_rules", {
	id: serial("id").primaryKey(),
	event: text$1("event").notNull(),
	action: text$1("action").notNull(),
	config: jsonb("config").$type(),
	enabled: boolean("enabled").notNull().default(true),
	remark: text$1("remark"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow()
});
const logs$2 = pgTable("logs", {
	id: serial("id").primaryKey(),
	level: text$1("level").notNull().default("info"),
	message: text$1("message").notNull(),
	details: text$1("details"),
	source: text$1("source"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const visitorProfiles$2 = pgTable("visitor_profiles", {
	visitorId: text$1("visitor_id").primaryKey(),
	userId: integer$1("user_id").references(() => users$2.id),
	ip: text$1("ip"),
	firstSeenAt: timestamp("first_seen_at", { withTimezone: true }).notNull().defaultNow(),
	lastSeenAt: timestamp("last_seen_at", { withTimezone: true }).notNull().defaultNow(),
	landingPath: text$1("landing_path"),
	firstPath: text$1("first_path"),
	lastPath: text$1("last_path"),
	firstReferrer: text$1("first_referrer"),
	lastReferrer: text$1("last_referrer"),
	firstSourceType: text$1("first_source_type"),
	lastSourceType: text$1("last_source_type"),
	firstSource: text$1("first_source"),
	lastSource: text$1("last_source"),
	firstMedium: text$1("first_medium"),
	lastMedium: text$1("last_medium"),
	firstCampaign: text$1("first_campaign"),
	lastCampaign: text$1("last_campaign"),
	firstContent: text$1("first_content"),
	lastContent: text$1("last_content"),
	firstTerm: text$1("first_term"),
	lastTerm: text$1("last_term"),
	country: text$1("country"),
	region: text$1("region"),
	city: text$1("city"),
	locale: text$1("locale"),
	currency: text$1("currency"),
	deviceType: text$1("device_type"),
	browser: text$1("browser"),
	os: text$1("os"),
	userAgent: text$1("user_agent"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
});
const visitorEvents$2 = pgTable("visitor_events", {
	id: serial("id").primaryKey(),
	visitorId: text$1("visitor_id").notNull(),
	ip: text$1("ip"),
	userId: integer$1("user_id").references(() => users$2.id),
	orderId: text$1("order_id").references(() => orders$2.id),
	productId: integer$1("product_id").references(() => products$2.id),
	eventName: text$1("event_name").notNull(),
	eventAction: text$1("event_action"),
	path: text$1("path"),
	referrer: text$1("referrer"),
	sourceType: text$1("source_type"),
	source: text$1("source"),
	medium: text$1("medium"),
	campaign: text$1("campaign"),
	content: text$1("content"),
	term: text$1("term"),
	country: text$1("country"),
	region: text$1("region"),
	city: text$1("city"),
	locale: text$1("locale"),
	currency: text$1("currency"),
	deviceType: text$1("device_type"),
	browser: text$1("browser"),
	os: text$1("os"),
	userAgent: text$1("user_agent"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const accessLogs$2 = pgTable("access_logs", {
	id: serial("id").primaryKey(),
	path: text$1("path").notNull(),
	method: text$1("method").notNull(),
	ip: text$1("ip"),
	userAgent: text$1("user_agent"),
	referrer: text$1("referrer"),
	country: text$1("country"),
	region: text$1("region"),
	city: text$1("city"),
	statusCode: integer$1("status_code"),
	duration: real$1("duration"),
	visitorId: text$1("visitor_id"),
	userId: integer$1("user_id").references(() => users$2.id),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const operationLogs$2 = pgTable("operation_logs", {
	id: serial("id").primaryKey(),
	actorType: text$1("actor_type").notNull().default("admin"),
	actorId: integer$1("actor_id"),
	actorName: text$1("actor_name"),
	action: text$1("action").notNull(),
	resource: text$1("resource").notNull(),
	resourceId: text$1("resource_id"),
	summary: text$1("summary"),
	details: text$1("details"),
	path: text$1("path").notNull(),
	method: text$1("method").notNull(),
	statusCode: integer$1("status_code"),
	ip: text$1("ip"),
	userAgent: text$1("user_agent"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
}, (table) => ({
	createdAtIdx: index$1("operation_logs_created_at_idx").on(table.createdAt),
	actorIdx: index$1("operation_logs_actor_idx").on(table.actorId, table.createdAt),
	resourceIdx: index$1("operation_logs_resource_idx").on(table.resource, table.resourceId)
}));
const posts$2 = pgTable("posts", {
	id: serial("id").primaryKey(),
	key: text$1("key"),
	sort: integer$1("sort"),
	slug: text$1("slug").notNull().unique(),
	title: text$1("title").notNull(),
	description: text$1("description"),
	content: text$1("content"),
	type: text$1("type").notNull().default("blog"),
	imageUrl: text$1("image_url"),
	views: integer$1("views").notNull().default(0),
	isActive: boolean("is_active").notNull().default(true),
	metaData: jsonb("meta_data"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow()
});
const notifications$2 = pgTable("notifications", {
	id: serial("id").primaryKey(),
	userId: integer$1("user_id").references(() => users$2.id),
	visitorId: text$1("visitor_id"),
	type: text$1("type").notNull(),
	title: text$1("title").notNull(),
	message: text$1("message").notNull(),
	data: jsonb("data"),
	isRead: boolean("is_read").notNull().default(false),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const balanceLogs$2 = pgTable("balance_logs", {
	id: serial("id").primaryKey(),
	userId: integer$1("user_id").notNull().references(() => users$2.id),
	walletId: integer$1("wallet_id").notNull().references(() => userWallets$2.id),
	balanceType: text$1("balance_type").notNull(),
	actionType: text$1("action_type").notNull().default("topup"),
	amountCents: bigint("amount_cents", { mode: "bigint" }).notNull(),
	beforeBalanceCents: bigint("before_balance_cents", { mode: "bigint" }).notNull(),
	afterBalanceCents: bigint("after_balance_cents", { mode: "bigint" }).notNull(),
	eventId: text$1("event_id").notNull().unique(),
	sourceType: text$1("source_type").notNull().default("system"),
	sourceId: text$1("source_id"),
	operatorAdminId: integer$1("operator_admin_id"),
	operatorName: text$1("operator_name").notNull().default(""),
	remark: text$1("remark").notNull().default(""),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
}, (table) => ({ walletCreatedAtIdx: index$1("idx_balance_logs_wallet_created_at").on(table.walletId, table.createdAt) }));
const promoAgentTiers$2 = pgTable("promo_agent_tiers", {
	id: serial("id").primaryKey(),
	code: text$1("code").notNull().unique(),
	name: text$1("name").notNull(),
	roleScope: text$1("role_scope").notNull().default("agent"),
	level: integer$1("level").notNull().default(1),
	discountRate: real$1("discount_rate").notNull().default(1),
	salesThreshold: real$1("sales_threshold").notNull().default(0),
	isFixed: boolean("is_fixed").notNull().default(false),
	isActive: boolean("is_active").notNull().default(true),
	description: text$1("description"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow()
});
const promoMembers$2 = pgTable("promo_members", {
	id: serial("id").primaryKey(),
	userId: integer$1("user_id").notNull().references(() => users$2.id).unique(),
	role: text$1("role").notNull().default("member"),
	status: text$1("status").notNull().default("active"),
	promoCode: text$1("promo_code").notNull().unique(),
	inviteCode: text$1("invite_code").notNull().unique(),
	agentCode: text$1("agent_code").unique(),
	currentAgentTierId: integer$1("current_agent_tier_id").references(() => promoAgentTiers$2.id),
	joinedAt: timestamp("joined_at", { withTimezone: true }).notNull().defaultNow(),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
});
const promoInviteRelations$2 = pgTable("promo_invite_relations", {
	id: serial("id").primaryKey(),
	inviteeUserId: integer$1("invitee_user_id").notNull().references(() => users$2.id).unique(),
	inviterUserId: integer$1("inviter_user_id").notNull().references(() => users$2.id),
	source: text$1("source").notNull().default("register"),
	codeSnapshot: text$1("code_snapshot"),
	boundAt: timestamp("bound_at", { withTimezone: true }).notNull().defaultNow(),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const promoAgentRelations$2 = pgTable("promo_agent_relations", {
	id: serial("id").primaryKey(),
	agentUserId: integer$1("agent_user_id").notNull().references(() => users$2.id).unique(),
	parentAgentUserId: integer$1("parent_agent_user_id").references(() => users$2.id),
	masterAgentUserId: integer$1("master_agent_user_id").references(() => users$2.id),
	depth: integer$1("depth").notNull().default(1),
	status: text$1("status").notNull().default("active"),
	boundAt: timestamp("bound_at", { withTimezone: true }).notNull().defaultNow(),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
});
const promoOrderAttributions$2 = pgTable("promo_order_attributions", {
	id: serial("id").primaryKey(),
	orderId: text$1("order_id").notNull().references(() => orders$2.id).unique(),
	buyerUserId: integer$1("buyer_user_id").references(() => users$2.id),
	buyerPromoMemberId: integer$1("buyer_promo_member_id").references(() => promoMembers$2.id),
	inviteUserId: integer$1("invite_user_id").references(() => users$2.id),
	agentUserId: integer$1("agent_user_id").references(() => users$2.id),
	parentAgentUserId: integer$1("parent_agent_user_id").references(() => users$2.id),
	masterAgentUserId: integer$1("master_agent_user_id").references(() => users$2.id),
	agentTierIdSnapshot: integer$1("agent_tier_id_snapshot"),
	agentTierNameSnapshot: text$1("agent_tier_name_snapshot"),
	discountRateSnapshot: real$1("discount_rate_snapshot"),
	sourceType: text$1("source_type").notNull().default("direct"),
	metaData: jsonb("meta_data"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
});
const promoCommissions$2 = pgTable("promo_commissions", {
	id: serial("id").primaryKey(),
	orderId: text$1("order_id").notNull().references(() => orders$2.id),
	ownerUserId: integer$1("owner_user_id").notNull().references(() => users$2.id),
	ownerPromoMemberId: integer$1("owner_promo_member_id").references(() => promoMembers$2.id),
	type: text$1("type").notNull(),
	sourceType: text$1("source_type").notNull().default("direct"),
	amount: real$1("amount").notNull(),
	rate: real$1("rate"),
	status: text$1("status").notNull().default("pending"),
	remark: text$1("remark"),
	metaData: jsonb("meta_data"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
}, (table) => ({ orderTypeIdx: uniqueIndex$1("promo_commissions_order_type_idx").on(table.orderId, table.type) }));
const promoApplications$2 = pgTable("promo_applications", {
	id: serial("id").primaryKey(),
	userId: integer$1("user_id").notNull().references(() => users$2.id),
	status: text$1("status").notNull().default("pending"),
	channelInfo: text$1("channel_info"),
	contact: text$1("contact"),
	reason: text$1("reason"),
	reviewNote: text$1("review_note"),
	reviewedByAdminId: integer$1("reviewed_by_admin_id"),
	reviewedAt: timestamp("reviewed_at", { withTimezone: true }),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
});
const tickets$2 = pgTable("tickets", {
	id: serial("id").primaryKey(),
	ticketNo: text$1("ticket_no").notNull().unique(),
	userId: integer$1("user_id").notNull().references(() => users$2.id),
	category: text$1("category").notNull().default("other"),
	title: text$1("title").notNull(),
	status: text$1("status").notNull().default("open"),
	priority: text$1("priority").notNull().default("normal"),
	context: jsonb("context"),
	lastRepliedAt: timestamp("last_replied_at", { withTimezone: true }).notNull().defaultNow(),
	lastRepliedBy: text$1("last_replied_by").notNull().default("user"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
	updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()
}, (table) => ({
	userIdIdx: index$1("tickets_user_id_idx").on(table.userId),
	statusIdx: index$1("tickets_status_idx").on(table.status),
	categoryIdx: index$1("tickets_category_idx").on(table.category),
	lastRepliedAtIdx: index$1("tickets_last_replied_at_idx").on(table.lastRepliedAt)
}));
const ticketMessages$2 = pgTable("ticket_messages", {
	id: serial("id").primaryKey(),
	ticketId: integer$1("ticket_id").notNull().references(() => tickets$2.id, { onDelete: "cascade" }),
	senderType: text$1("sender_type").notNull(),
	senderId: integer$1("sender_id"),
	senderName: text$1("sender_name").notNull().default(""),
	content: text$1("content").notNull(),
	attachments: jsonb("attachments"),
	createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()
}, (table) => ({
	ticketIdIdx: index$1("ticket_messages_ticket_id_idx").on(table.ticketId),
	createdAtIdx: index$1("ticket_messages_created_at_idx").on(table.createdAt)
}));

//#endregion
//#region server/db/schema.mysql.ts
var schema_mysql_exports = /* @__PURE__ */ __exportAll({
	accessLogs: () => accessLogs$1,
	adminTokens: () => adminTokens$1,
	admins: () => admins$1,
	balanceLogs: () => balanceLogs$1,
	cards: () => cards$1,
	emailLogs: () => emailLogs$1,
	emailProviders: () => emailProviders$1,
	eventRules: () => eventRules$1,
	failures: () => failures$1,
	logs: () => logs$1,
	notifications: () => notifications$1,
	oauthAccounts: () => oauthAccounts$1,
	operationLogs: () => operationLogs$1,
	orders: () => orders$1,
	paymentFailures: () => paymentFailures$1,
	paymentMethods: () => paymentMethods$1,
	posts: () => posts$1,
	products: () => products$1,
	promoAgentRelations: () => promoAgentRelations$1,
	promoAgentTiers: () => promoAgentTiers$1,
	promoApplications: () => promoApplications$1,
	promoCommissions: () => promoCommissions$1,
	promoInviteRelations: () => promoInviteRelations$1,
	promoMembers: () => promoMembers$1,
	promoOrderAttributions: () => promoOrderAttributions$1,
	settings: () => settings$1,
	subscriptions: () => subscriptions$1,
	ticketMessages: () => ticketMessages$1,
	tickets: () => tickets$1,
	topups: () => topups$1,
	userSessions: () => userSessions$1,
	userTokens: () => userTokens$1,
	userWallets: () => userWallets$1,
	users: () => users$1,
	visitorEvents: () => visitorEvents$1,
	visitorProfiles: () => visitorProfiles$1,
	webhooks: () => webhooks$1
});
const users$1 = mysqlTable("users", {
	id: int("id").autoincrement().primaryKey(),
	email: varchar("email", { length: 191 }).notNull().unique(),
	passwordHash: text$2("password_hash"),
	nickname: text$2("nickname"),
	avatarUrl: text$2("avatar_url"),
	lastLoginAt: timestamp$1("last_login_at"),
	currentSessionId: text$2("current_session_id"),
	status: int("status").default(1),
	emailVerifiedAt: timestamp$1("email_verified_at"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const userWallets$1 = mysqlTable("user_wallets", {
	id: int("id").autoincrement().primaryKey(),
	userId: int("user_id").notNull().references(() => users$1.id, { onDelete: "cascade" }).unique(),
	cashBalance: bigint$1("cash_balance", { mode: "bigint" }).notNull().default(sql`0`),
	grantBalance: bigint$1("grant_balance", { mode: "bigint" }).notNull().default(sql`0`),
	subBalance: bigint$1("sub_balance", { mode: "bigint" }).notNull().default(sql`0`),
	pointsBalance: bigint$1("points_balance", { mode: "bigint" }).notNull().default(sql`0`),
	tierLevel: int("tier_level").notNull().default(0),
	subExpiresAt: timestamp$1("sub_expires_at"),
	status: int("status").notNull().default(1),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const userSessions$1 = mysqlTable("user_sessions", {
	id: int("id").autoincrement().primaryKey(),
	userId: int("user_id").notNull().references(() => users$1.id),
	sessionIdHash: varchar("session_id_hash", { length: 64 }).notNull().unique(),
	status: varchar("status", { length: 32 }).notNull().default("active"),
	authMethod: varchar("auth_method", { length: 32 }).notNull().default("password"),
	deviceType: varchar("device_type", { length: 32 }),
	browser: varchar("browser", { length: 64 }),
	os: varchar("os", { length: 64 }),
	userAgent: text$2("user_agent"),
	ip: varchar("ip", { length: 64 }),
	country: varchar("country", { length: 100 }),
	region: varchar("region", { length: 100 }),
	city: varchar("city", { length: 100 }),
	loggedInAt: timestamp$1("logged_in_at").notNull().defaultNow(),
	lastSeenAt: timestamp$1("last_seen_at").notNull().defaultNow(),
	endedAt: timestamp$1("ended_at"),
	replacedBySessionId: varchar("replaced_by_session_id", { length: 64 }),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
}, (table) => ({
	userStatusIdx: index$2("user_sessions_user_status_idx").on(table.userId, table.status),
	lastSeenIdx: index$2("user_sessions_last_seen_idx").on(table.lastSeenAt)
}));
const userTokens$1 = mysqlTable("user_tokens", {
	id: int("id").autoincrement().primaryKey(),
	userId: int("user_id").notNull().references(() => users$1.id),
	token: varchar("token", { length: 191 }).notNull().unique(),
	name: text$2("name"),
	expiresAt: timestamp$1("expires_at"),
	lastUsedAt: timestamp$1("last_used_at"),
	revoked: boolean$1("revoked").notNull().default(false),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const admins$1 = mysqlTable("admins", {
	id: int("id").autoincrement().primaryKey(),
	username: varchar("username", { length: 191 }).notNull().unique(),
	passwordHash: varchar("password_hash", { length: 255 }).notNull(),
	permissions: json("permissions").$type(),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const adminTokens$1 = mysqlTable("admin_tokens", {
	id: int("id").autoincrement().primaryKey(),
	adminId: int("admin_id").notNull().references(() => admins$1.id),
	token: varchar("token", { length: 191 }).notNull().unique(),
	name: text$2("name"),
	permissions: json("permissions").$type(),
	expiresAt: timestamp$1("expires_at"),
	lastUsedAt: timestamp$1("last_used_at"),
	revoked: boolean$1("revoked").notNull().default(false),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const oauthAccounts$1 = mysqlTable("oauth_accounts", {
	id: int("id").autoincrement().primaryKey(),
	userId: int("user_id").notNull().references(() => users$1.id),
	provider: varchar("provider", { length: 191 }).notNull(),
	providerAccountId: varchar("provider_account_id", { length: 255 }).notNull(),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
}, (table) => {
	return { providerIdx: uniqueIndex$2("provider_account_idx").on(table.provider, table.providerAccountId) };
});
const products$1 = mysqlTable("products", {
	id: int("id").autoincrement().primaryKey(),
	slug: varchar("slug", { length: 191 }).unique(),
	name: text$2("name").notNull(),
	price: real$2("price").notNull(),
	description: text$2("description"),
	content: text$2("content"),
	type: text$2("type").notNull(),
	imageUrl: text$2("image_url"),
	views: int("views").notNull().default(0),
	imageUrls: json("image_urls").$type(),
	resource: text$2("resource"),
	isActive: boolean$1("is_active").notNull().default(true),
	status: varchar("status", { length: 32 }).notNull().default("active"),
	metaData: json("meta_data"),
	sortOrder: int("sort_order").notNull().default(0),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const cards$1 = mysqlTable("cards", {
	id: int("id").autoincrement().primaryKey(),
	productId: int("product_id").notNull().references(() => products$1.id),
	cardNumber: text$2("card_number").notNull(),
	isUsed: boolean$1("is_used").notNull().default(false),
	orderId: text$2("order_id"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const paymentMethods$1 = mysqlTable("payment_methods", {
	id: int("id").autoincrement().primaryKey(),
	name: text$2("name").notNull(),
	code: text$2("code").notNull(),
	iconUrl: text$2("icon_url"),
	isActive: boolean$1("is_active").notNull().default(false),
	supportedLocales: text$2("supported_locales"),
	configJson: text$2("config_json"),
	info: text$2("info"),
	create: text$2("create"),
	callback: text$2("callback"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const emailProviders$1 = mysqlTable("email_providers", {
	id: int("id").autoincrement().primaryKey(),
	name: text$2("name").notNull(),
	code: text$2("code").notNull(),
	isActive: boolean$1("is_active").notNull().default(false),
	configJson: text$2("config_json"),
	sendScript: text$2("send_script"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const emailLogs$1 = mysqlTable("email_logs", {
	id: int("id").autoincrement().primaryKey(),
	to: text$2("to").notNull(),
	subject: text$2("subject").notNull(),
	templateCode: text$2("template_code"),
	html: text$2("html"),
	provider: text$2("provider"),
	status: text$2("status").notNull().default("success"),
	messageId: text$2("message_id"),
	error: text$2("error"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const orders$1 = mysqlTable("orders", {
	id: varchar("id", { length: 191 }).primaryKey(),
	amount: real$2("amount").notNull(),
	currency: text$2("currency").notNull().default("USD"),
	source: varchar("source", { length: 64 }),
	externalOrderId: varchar("external_order_id", { length: 128 }),
	productId: int("product_id").notNull().references(() => products$1.id),
	userId: int("user_id").references(() => users$1.id),
	contactEmail: text$2("contact_email").notNull(),
	payMethod: text$2("pay_method"),
	tradeNo: text$2("trade_no"),
	status: text$2("status").notNull().default("none"),
	deliveryInfo: text$2("delivery_info"),
	metaData: json("meta_data"),
	visitorId: text$2("visitor_id"),
	subscriptionId: text$2("subscription_id"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	paidAt: timestamp$1("paid_at"),
	payStatus: text$2("pay_status").notNull().default("pending")
}, (table) => [uniqueIndex$2("orders_source_external_order_unique").on(table.source, table.externalOrderId)]);
const topups$1 = mysqlTable("topups", {
	id: int("id").autoincrement().primaryKey(),
	orderId: varchar("order_id", { length: 191 }).notNull().unique(),
	userId: int("user_id").notNull().references(() => users$1.id),
	walletId: int("wallet_id").notNull().references(() => userWallets$1.id),
	source: varchar("source", { length: 32 }).notNull().default("order"),
	paymentAmount: real$2("payment_amount").notNull(),
	paymentCurrency: varchar("payment_currency", { length: 16 }).notNull(),
	creditAmountCents: bigint$1("credit_amount_cents", { mode: "bigint" }).notNull(),
	creditCurrency: varchar("credit_currency", { length: 16 }).notNull(),
	exchangeRate: real$2("exchange_rate").notNull().default(1),
	balanceType: varchar("balance_type", { length: 20 }).notNull().default("cash"),
	status: varchar("status", { length: 32 }).notNull().default("pending"),
	creditEventId: varchar("credit_event_id", { length: 255 }).notNull().unique(),
	refundEventId: varchar("refund_event_id", { length: 255 }).unique(),
	retryCount: int("retry_count").notNull().default(0),
	shortfallCents: bigint$1("shortfall_cents", { mode: "bigint" }).notNull().default(sql`0`),
	lastError: text$2("last_error"),
	paidAt: timestamp$1("paid_at"),
	creditedAt: timestamp$1("credited_at"),
	refundedAt: timestamp$1("refunded_at"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").notNull().defaultNow()
}, (table) => ({
	userCreatedAtIdx: index$2("idx_topups_user_created_at").on(table.userId, table.createdAt),
	statusUpdatedAtIdx: index$2("idx_topups_status_updated_at").on(table.status, table.updatedAt)
}));
const subscriptions$1 = mysqlTable("subscriptions", {
	id: varchar("id", { length: 191 }).primaryKey(),
	gatewaySubId: text$2("gateway_sub_id"),
	userId: int("user_id").references(() => users$1.id),
	productId: int("product_id").notNull().references(() => products$1.id),
	payMethod: text$2("pay_method").notNull(),
	status: text$2("status").notNull().default("active"),
	interval: text$2("interval").notNull(),
	intervalCount: int("interval_count").notNull().default(1),
	amount: real$2("amount").notNull(),
	currency: text$2("currency").notNull().default("USD"),
	currentPeriodStart: timestamp$1("current_period_start"),
	currentPeriodEnd: timestamp$1("current_period_end"),
	cancelAtPeriodEnd: boolean$1("cancel_at_period_end").default(false),
	metaData: json("meta_data"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").defaultNow()
});
const settings$1 = mysqlTable("settings", {
	key: varchar("key", { length: 191 }).primaryKey(),
	value: text$2("value").notNull(),
	description: text$2("description"),
	updatedAt: timestamp$1("updated_at").defaultNow()
});
const paymentFailures$1 = mysqlTable("payment_failures", {
	id: int("id").autoincrement().primaryKey(),
	orderId: text$2("order_id").notNull(),
	cardBin: text$2("card_bin"),
	reason: text$2("reason").notNull(),
	amount: real$2("amount"),
	payMethod: text$2("pay_method"),
	contactEmail: text$2("contact_email"),
	rawResponse: text$2("raw_response"),
	visitorId: text$2("visitor_id"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const failures$1 = paymentFailures$1;
const webhooks$1 = mysqlTable("webhooks", {
	id: int("id").autoincrement().primaryKey(),
	name: text$2("name").notNull(),
	url: text$2("url").notNull(),
	events: json("events").$type(),
	secret: text$2("secret"),
	isActive: boolean$1("is_active").notNull().default(true),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const eventRules$1 = mysqlTable("event_rules", {
	id: int("id").autoincrement().primaryKey(),
	event: text$2("event").notNull(),
	action: text$2("action").notNull(),
	config: json("config").$type(),
	enabled: boolean$1("enabled").notNull().default(true),
	remark: text$2("remark"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").defaultNow()
});
const logs$1 = mysqlTable("logs", {
	id: int("id").autoincrement().primaryKey(),
	level: text$2("level").notNull().default("info"),
	message: text$2("message").notNull(),
	details: text$2("details"),
	source: text$2("source"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const visitorProfiles$1 = mysqlTable("visitor_profiles", {
	visitorId: varchar("visitor_id", { length: 191 }).primaryKey(),
	userId: int("user_id").references(() => users$1.id),
	ip: text$2("ip"),
	firstSeenAt: timestamp$1("first_seen_at").notNull().defaultNow(),
	lastSeenAt: timestamp$1("last_seen_at").notNull().defaultNow(),
	landingPath: text$2("landing_path"),
	firstPath: text$2("first_path"),
	lastPath: text$2("last_path"),
	firstReferrer: text$2("first_referrer"),
	lastReferrer: text$2("last_referrer"),
	firstSourceType: text$2("first_source_type"),
	lastSourceType: text$2("last_source_type"),
	firstSource: text$2("first_source"),
	lastSource: text$2("last_source"),
	firstMedium: text$2("first_medium"),
	lastMedium: text$2("last_medium"),
	firstCampaign: text$2("first_campaign"),
	lastCampaign: text$2("last_campaign"),
	firstContent: text$2("first_content"),
	lastContent: text$2("last_content"),
	firstTerm: text$2("first_term"),
	lastTerm: text$2("last_term"),
	country: text$2("country"),
	region: text$2("region"),
	city: text$2("city"),
	locale: text$2("locale"),
	currency: text$2("currency"),
	deviceType: text$2("device_type"),
	browser: text$2("browser"),
	os: text$2("os"),
	userAgent: text$2("user_agent"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").notNull().defaultNow()
});
const visitorEvents$1 = mysqlTable("visitor_events", {
	id: int("id").autoincrement().primaryKey(),
	visitorId: text$2("visitor_id").notNull(),
	ip: text$2("ip"),
	userId: int("user_id").references(() => users$1.id),
	orderId: varchar("order_id", { length: 191 }).references(() => orders$1.id),
	productId: int("product_id").references(() => products$1.id),
	eventName: text$2("event_name").notNull(),
	eventAction: text$2("event_action"),
	path: text$2("path"),
	referrer: text$2("referrer"),
	sourceType: text$2("source_type"),
	source: text$2("source"),
	medium: text$2("medium"),
	campaign: text$2("campaign"),
	content: text$2("content"),
	term: text$2("term"),
	country: text$2("country"),
	region: text$2("region"),
	city: text$2("city"),
	locale: text$2("locale"),
	currency: text$2("currency"),
	deviceType: text$2("device_type"),
	browser: text$2("browser"),
	os: text$2("os"),
	userAgent: text$2("user_agent"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const accessLogs$1 = mysqlTable("access_logs", {
	id: int("id").autoincrement().primaryKey(),
	path: text$2("path").notNull(),
	method: text$2("method").notNull(),
	ip: text$2("ip"),
	userAgent: text$2("user_agent"),
	referrer: text$2("referrer"),
	country: text$2("country"),
	region: text$2("region"),
	city: text$2("city"),
	statusCode: int("status_code"),
	duration: real$2("duration"),
	visitorId: text$2("visitor_id"),
	userId: int("user_id").references(() => users$1.id),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const operationLogs$1 = mysqlTable("operation_logs", {
	id: int("id").autoincrement().primaryKey(),
	actorType: varchar("actor_type", { length: 32 }).notNull().default("admin"),
	actorId: int("actor_id"),
	actorName: varchar("actor_name", { length: 191 }),
	action: varchar("action", { length: 64 }).notNull(),
	resource: varchar("resource", { length: 64 }).notNull(),
	resourceId: varchar("resource_id", { length: 191 }),
	summary: text$2("summary"),
	details: text$2("details"),
	path: text$2("path").notNull(),
	method: varchar("method", { length: 16 }).notNull(),
	statusCode: int("status_code"),
	ip: varchar("ip", { length: 64 }),
	userAgent: text$2("user_agent"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
}, (table) => ({
	createdAtIdx: index$2("operation_logs_created_at_idx").on(table.createdAt),
	actorIdx: index$2("operation_logs_actor_idx").on(table.actorId, table.createdAt),
	resourceIdx: index$2("operation_logs_resource_idx").on(table.resource, table.resourceId)
}));
const posts$1 = mysqlTable("posts", {
	id: int("id").autoincrement().primaryKey(),
	key: text$2("key"),
	sort: int("sort"),
	slug: varchar("slug", { length: 191 }).notNull().unique(),
	title: text$2("title").notNull(),
	description: text$2("description"),
	content: text$2("content"),
	type: text$2("type").notNull().default("blog"),
	imageUrl: text$2("image_url"),
	views: int("views").notNull().default(0),
	isActive: boolean$1("is_active").notNull().default(true),
	metaData: json("meta_data"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").defaultNow()
});
const notifications$1 = mysqlTable("notifications", {
	id: int("id").autoincrement().primaryKey(),
	userId: int("user_id").references(() => users$1.id),
	visitorId: text$2("visitor_id"),
	type: text$2("type").notNull(),
	title: text$2("title").notNull(),
	message: text$2("message").notNull(),
	data: json("data"),
	isRead: boolean$1("is_read").notNull().default(false),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const balanceLogs$1 = mysqlTable("balance_logs", {
	id: int("id").primaryKey().autoincrement(),
	userId: int("user_id").notNull().references(() => users$1.id),
	walletId: int("wallet_id").notNull().references(() => userWallets$1.id),
	balanceType: varchar("balance_type", { length: 20 }).notNull(),
	actionType: varchar("action_type", { length: 50 }).notNull().default("topup"),
	amountCents: bigint$1("amount_cents", { mode: "bigint" }).notNull(),
	beforeBalanceCents: bigint$1("before_balance_cents", { mode: "bigint" }).notNull(),
	afterBalanceCents: bigint$1("after_balance_cents", { mode: "bigint" }).notNull(),
	eventId: varchar("event_id", { length: 191 }).notNull().unique(),
	sourceType: varchar("source_type", { length: 32 }).notNull().default("system"),
	sourceId: varchar("source_id", { length: 191 }),
	operatorAdminId: int("operator_admin_id"),
	operatorName: varchar("operator_name", { length: 100 }).notNull().default(""),
	remark: text$2("remark"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
}, (table) => ({ walletCreatedAtIdx: index$2("idx_balance_logs_wallet_created_at").on(table.walletId, table.createdAt) }));
const promoAgentTiers$1 = mysqlTable("promo_agent_tiers", {
	id: int("id").autoincrement().primaryKey(),
	code: varchar("code", { length: 191 }).notNull().unique(),
	name: varchar("name", { length: 191 }).notNull(),
	roleScope: varchar("role_scope", { length: 64 }).notNull().default("agent"),
	level: int("level").notNull().default(1),
	discountRate: real$2("discount_rate").notNull().default(1),
	salesThreshold: real$2("sales_threshold").notNull().default(0),
	isFixed: boolean$1("is_fixed").notNull().default(false),
	isActive: boolean$1("is_active").notNull().default(true),
	description: text$2("description"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").defaultNow()
});
const promoMembers$1 = mysqlTable("promo_members", {
	id: int("id").autoincrement().primaryKey(),
	userId: int("user_id").notNull().references(() => users$1.id).unique(),
	role: varchar("role", { length: 64 }).notNull().default("member"),
	status: varchar("status", { length: 64 }).notNull().default("active"),
	promoCode: varchar("promo_code", { length: 191 }).notNull().unique(),
	inviteCode: varchar("invite_code", { length: 191 }).notNull().unique(),
	agentCode: varchar("agent_code", { length: 191 }).unique(),
	currentAgentTierId: int("current_agent_tier_id").references(() => promoAgentTiers$1.id),
	joinedAt: timestamp$1("joined_at").notNull().defaultNow(),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").notNull().defaultNow()
});
const promoInviteRelations$1 = mysqlTable("promo_invite_relations", {
	id: int("id").autoincrement().primaryKey(),
	inviteeUserId: int("invitee_user_id").notNull().references(() => users$1.id).unique(),
	inviterUserId: int("inviter_user_id").notNull().references(() => users$1.id),
	source: varchar("source", { length: 64 }).notNull().default("register"),
	codeSnapshot: varchar("code_snapshot", { length: 191 }),
	boundAt: timestamp$1("bound_at").notNull().defaultNow(),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
});
const promoAgentRelations$1 = mysqlTable("promo_agent_relations", {
	id: int("id").autoincrement().primaryKey(),
	agentUserId: int("agent_user_id").notNull().references(() => users$1.id).unique(),
	parentAgentUserId: int("parent_agent_user_id").references(() => users$1.id),
	masterAgentUserId: int("master_agent_user_id").references(() => users$1.id),
	depth: int("depth").notNull().default(1),
	status: varchar("status", { length: 64 }).notNull().default("active"),
	boundAt: timestamp$1("bound_at").notNull().defaultNow(),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").notNull().defaultNow()
});
const promoOrderAttributions$1 = mysqlTable("promo_order_attributions", {
	id: int("id").autoincrement().primaryKey(),
	orderId: varchar("order_id", { length: 191 }).notNull().references(() => orders$1.id),
	buyerUserId: int("buyer_user_id").references(() => users$1.id),
	buyerPromoMemberId: int("buyer_promo_member_id"),
	inviteUserId: int("invite_user_id").references(() => users$1.id),
	agentUserId: int("agent_user_id").references(() => users$1.id),
	parentAgentUserId: int("parent_agent_user_id").references(() => users$1.id),
	masterAgentUserId: int("master_agent_user_id").references(() => users$1.id),
	agentTierIdSnapshot: int("agent_tier_id_snapshot"),
	agentTierNameSnapshot: varchar("agent_tier_name_snapshot", { length: 191 }),
	discountRateSnapshot: real$2("discount_rate_snapshot"),
	sourceType: varchar("source_type", { length: 64 }).notNull().default("direct"),
	metaData: json("meta_data"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
}, (table) => {
	return {
		orderIdx: uniqueIndex$2("promo_order_attributions_order_id_idx").on(table.orderId),
		buyerPromoMemberFk: foreignKey({
			name: "promo_order_attr_buyer_member_fk",
			columns: [table.buyerPromoMemberId],
			foreignColumns: [promoMembers$1.id]
		})
	};
});
const promoCommissions$1 = mysqlTable("promo_commissions", {
	id: int("id").autoincrement().primaryKey(),
	orderId: varchar("order_id", { length: 191 }).notNull().references(() => orders$1.id),
	ownerUserId: int("owner_user_id").notNull().references(() => users$1.id),
	ownerPromoMemberId: int("owner_promo_member_id").references(() => promoMembers$1.id),
	type: varchar("type", { length: 64 }).notNull(),
	sourceType: varchar("source_type", { length: 64 }).notNull().default("direct"),
	amount: real$2("amount").notNull(),
	rate: real$2("rate"),
	status: varchar("status", { length: 64 }).notNull().default("pending"),
	remark: text$2("remark"),
	metaData: json("meta_data"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").notNull().defaultNow()
}, (table) => {
	return { orderTypeIdx: uniqueIndex$2("promo_commissions_order_type_idx").on(table.orderId, table.type) };
});
const promoApplications$1 = mysqlTable("promo_applications", {
	id: int("id").autoincrement().primaryKey(),
	userId: int("user_id").notNull().references(() => users$1.id),
	status: varchar("status", { length: 64 }).notNull().default("pending"),
	channelInfo: text$2("channel_info"),
	contact: text$2("contact"),
	reason: text$2("reason"),
	reviewNote: text$2("review_note"),
	reviewedByAdminId: int("reviewed_by_admin_id"),
	reviewedAt: timestamp$1("reviewed_at"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").notNull().defaultNow()
});
const tickets$1 = mysqlTable("tickets", {
	id: int("id").autoincrement().primaryKey(),
	ticketNo: varchar("ticket_no", { length: 64 }).notNull().unique(),
	userId: int("user_id").notNull().references(() => users$1.id),
	category: varchar("category", { length: 64 }).notNull().default("other"),
	title: text$2("title").notNull(),
	status: varchar("status", { length: 32 }).notNull().default("open"),
	priority: varchar("priority", { length: 32 }).notNull().default("normal"),
	context: json("context"),
	lastRepliedAt: timestamp$1("last_replied_at").notNull().defaultNow(),
	lastRepliedBy: varchar("last_replied_by", { length: 32 }).notNull().default("user"),
	createdAt: timestamp$1("created_at").notNull().defaultNow(),
	updatedAt: timestamp$1("updated_at").notNull().defaultNow()
}, (table) => ({
	userIdIdx: index$2("tickets_user_id_idx").on(table.userId),
	statusIdx: index$2("tickets_status_idx").on(table.status),
	categoryIdx: index$2("tickets_category_idx").on(table.category),
	lastRepliedAtIdx: index$2("tickets_last_replied_at_idx").on(table.lastRepliedAt)
}));
const ticketMessages$1 = mysqlTable("ticket_messages", {
	id: int("id").autoincrement().primaryKey(),
	ticketId: int("ticket_id").notNull().references(() => tickets$1.id, { onDelete: "cascade" }),
	senderType: varchar("sender_type", { length: 32 }).notNull(),
	senderId: int("sender_id"),
	senderName: varchar("sender_name", { length: 128 }).notNull().default(""),
	content: text$2("content").notNull(),
	attachments: json("attachments"),
	createdAt: timestamp$1("created_at").notNull().defaultNow()
}, (table) => ({
	ticketIdIdx: index$2("ticket_messages_ticket_id_idx").on(table.ticketId),
	createdAtIdx: index$2("ticket_messages_created_at_idx").on(table.createdAt)
}));

//#endregion
//#region server/db/schema.ts
const resolveDialect = () => {
	const explicitDialect = process.env.DB_DIALECT?.replace(/"/g, "").toLowerCase();
	if (explicitDialect === "postgresql" || explicitDialect === "sqlite" || explicitDialect === "mysql") return explicitDialect;
	const connectionUrl = process.env.DATABASE_URL || process.env.POSTGRES_URL || process.env.POSTGRESQL_URL || process.env.NUXT_DATABASE_URL || process.env.LIBSQL_URL || "";
	if (connectionUrl.startsWith("postgres://") || connectionUrl.startsWith("postgresql://")) return "postgresql";
	if (connectionUrl.startsWith("mysql://")) return "mysql";
	return "sqlite";
};
const resolveSchema = () => {
	const dialect = resolveDialect();
	if (dialect === "postgresql") return schema_pg_exports;
	if (dialect === "mysql") return schema_mysql_exports;
	return schema_sqlite_exports;
};
const activeSchema = resolveSchema();
const { users, userWallets, userSessions, userTokens, admins, adminTokens, oauthAccounts, products, cards, paymentMethods, emailProviders, emailLogs, orders, topups, settings, paymentFailures, failures, webhooks, eventRules, logs, visitorProfiles, visitorEvents, accessLogs, operationLogs, posts, subscriptions, notifications, balanceLogs, promoMembers, promoInviteRelations, promoAgentRelations, promoAgentTiers, promoOrderAttributions, promoCommissions, promoApplications, tickets, ticketMessages } = activeSchema;

//#endregion